{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["clean", "^build"],
      "outputs": ["dist/**", ".sonda/**"]
    },
    "dev": {
      "dependsOn": ["build"],
      "persistent": true,
      "cache": false
    }
  }
}
