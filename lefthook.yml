pre-commit:
  parallel: true
  fail_on_changes: "always"
  commands:
    lint:
      run: npm run lint:fix
    typecheck:
      run: npm run check
commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}
