pre-commit:
  parallel: true
  fail_on_changes: "always"
  commands:
    lint:
      run: npm run sdk:lint:fix
    typecheck:
      run: npm run sdk:check:tsc
commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}
