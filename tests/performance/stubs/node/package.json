{
  "name": "@sentry/node",
  "version": "9.999.0",
  "private": true,
  "description": "Empty stub to keep this dependency and its OpenTelemetry transitive deps out of the install tree. Lighthouse only imports it when enableErrorReporting is true, which the perf suite explicitly disables, so this package has no runtime code."
}
