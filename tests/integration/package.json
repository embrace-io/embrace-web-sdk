{
  "name": "tests-integration",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install-dependencies": "bash scripts/installE2EPlatformDependencies.sh",
    "build": "npx tsx --test \"tests/build-platform/*.test.ts\" --test-timeout=20000",
    "test": "npm run install-dependencies && npm run build && npx playwright test",
    "update-golden": "UPDATE_GOLDEN=1 npm run test"
  }
}
