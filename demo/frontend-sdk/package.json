{
  "name": "frontend-sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "setup": "cd sdk && npm install && cd ../host && npm install && cd .. && bash scripts/setupBundleSymLinks.sh",
    "build": "cd sdk && npm run build && cd ../host && npm run build",
    "dev": "cd host && npm run dev",
    "serve": "cd host && npx serve dist"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "7.1.2"
  },
  "workspaces": [
    "sdk",
    "host"
  ]
}
