{
  "name": "frontend-sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "setup": "npm run sdk:compile --prefix ../.. && bash scripts/setupBundleSymLinks.sh",
    "build": "tsc && vite build",
    "dev": "vite",
    "serve": "npm run build && vite preview"
  },
  "dependencies": {
    "@opentelemetry/sdk-logs": "0.209.0",
    "@opentelemetry/sdk-trace-web": "2.3.0"
  }
}
