{
  "name": "frontend-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "npx turbo run build && bash scripts/setupBundleSymLinks.sh",
    "build": "tsc && vite build",
    "dev": "vite",
    "serve": "npm run build && vite preview"
  },
  "dependencies": {
    "@embrace-io/web-sdk": "*",
    "@opentelemetry/sdk-logs": "^0.220.0",
    "@opentelemetry/sdk-trace-web": "^2.9.0"
  }
}
